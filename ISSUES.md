# ISSUES.md — Combined Architectural Issue Registry

Unified view of all architectural/systemic issues identified across the `rumo` project.
Each issue has a unique sequential ID. Sorted descending by severity (critical → high → medium → low).

**Sources:**
- `FIXME.md` — root package + `cmd` CLI
- `vm/FIXME.md` — VM, compiler, object system, concurrency
- `std/FIXME.md` — stdlib module framework + cross-module concerns

**Severity:** critical (crashes, data races, build failures under normal use) · high (subtle bugs, silent misbehaviour) · medium (performance, maintainability, usability) · low (minor improvements)

**Related issues** are noted with `→ #N` cross-references.

---

| ID | Title                                                                         | Description                                                                                                                                                                                                                                                                        | Category          | Severity     |
|----|-------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------|--------------|
| 1  | ~~Shared `globals` slice in `ShallowClone` (data race)~~                      | ~~`ShallowClone()` copies the pointer to the same `[]Object` globals slice. Parent and child VMs read/write globals (`OpSetGlobal`/`OpGetGlobal`) without synchronisation. Textbook data race. (`vm/FIXME.md` §1) → #2, #3, #14~~                                                  | ~~Thread Safety~~ | ~~critical~~ |
| 2  | ~~Shared `constants` with mutable `*ObjectPtr` cells (data race)~~            | ~~`ShallowClone()` shares the `constants` slice. `*CompiledFunction.Free` contains `*ObjectPtr` cells mutated at runtime via `OpSetFree`. Two VMs executing the same closure race on closed-over variables. (`vm/FIXME.md` §2) → #1, #3~~                                          | ~~Thread Safety~~ | ~~critical~~ |
| 3  | ~~`ShallowClone` context stores parent VM pointer~~                           | ~~`context.WithValue(v.ctx, ContextKey("vm"), v)` stores the **parent**, not the clone. Builtins in the child VM (e.g. `abort()`, `start()`) operate on the wrong VM, breaking abort propagation and child tracking. (`vm/FIXME.md` §3) → #1, #2, #14~~                            | ~~Thread Safety~~ | ~~critical~~ |
| 4  | ~~`routineVM.abort()` races with goroutine completion~~                       | ~~`gvm.VM` is read in `abort()` and nilled in the goroutine's deferred cleanup without synchronisation. Nil-pointer dereference crash. (`vm/FIXME.md` §4)~~                                                                                                                        | ~~Thread Safety~~ | ~~critical~~ |
| 5  | ~~`wait()` channel is one-shot — concurrent callers deadlock~~                | ~~`waitChan` capacity 1. Two concurrent `wait()`/`result()` calls that both see `done == 0` enter `select` — only one succeeds, the other blocks forever. (`vm/FIXME.md` §5)~~                                                                                                     | ~~Thread Safety~~ | ~~critical~~ |
| 6  | ~~Channel `close()` panics are unrecoverable~~                                | ~~Double-close or send-on-closed-channel `panic`s the goroutine. The `builtinStart` defer's `callers == nil` check itself panics for non-compiled callables. (`vm/FIXME.md` §6)~~                                                                                                  | ~~Thread Safety~~ | ~~critical~~ |
| 7  | ~~Non-compiled callables use parent context — not abortable~~                 | ~~Non-compiled callables in `builtinStart` receive the parent VM's context and have no independent `cancel()`. `gvm.abort()` is a no-op for them. (`vm/FIXME.md` §7) → #8~~                                                                                                        | ~~Thread Safety~~ | ~~critical~~ |
| 8  | ~~`addChild` with `nil` VM — non-compiled routines unkillable~~               | ~~Non-compiled routines increment the WaitGroup but are never added to `vmMap`, making them impossible to abort via `Abort()`. (`vm/FIXME.md` §8) → #7~~                                                                                                                           | ~~Thread Safety~~ | ~~critical~~ |
| 9  | ~~`Program.Run()`/`RunContext()` hold write lock during execution~~           | ~~The `Program` write lock is held for the entire script lifetime. `Get()`/`Set()`/`IsDefined()` block until the script finishes — effectively deadlocked for long-running scripts. (`FIXME.md` §1.1) → #10~~                                                                      | ~~Thread Safety~~ | ~~critical~~ |
| 10 | ~~`Program.Clone()` shares mutable globals and constants~~                    | ~~`Clone()` copies object pointers, not objects. Shared `*vm.Map`/`*vm.Array` globals race under concurrent execution. `bytecode.Constants` with `CompiledFunction.Free` is also shared. (`FIXME.md` §1.2) → #1, #2, #9~~                                                          | ~~Thread Safety~~ | ~~critical~~ |
| 11 | ~~Eager init of `Modules`/`Exports` forces all stdlib into every binary~~     | ~~Package-level `var Modules = GetModuleMap(AllModuleNames()...)` triggers `init()` in all stdlib packages at import time. Binary size, startup time, memory cost for unused modules. (`FIXME.md` §2.1) → #22~~                                                                    | ~~Integration~~   | ~~critical~~ |
| 13 | ~~JSON decoder panics on malformed internal state — no recovery~~             | ~~Seven `panic(phasePanicMsg)` calls in `json/decode.go` with zero `recover()` anywhere. A scanner state machine bug crashes the entire process. (`std/FIXME.md` §1)~~                                                                                                             | ~~Correctness~~   | ~~critical~~ |
| 25 | ~~`Map` and `Array` mutations are not thread-safe~~                               | ~~`IndexSet`, `builtinAppend`, `builtinDelete`, `builtinSplice` mutate underlying Go map/slice directly. Shared objects between routines cause data races. (`vm/FIXME.md` §9) → #1~~                                                                                                   | ~~Thread Safety~~     | ~~high~~         |
| 26 | ~~`Int` division/modulo by zero unguarded~~                                       | ~~`BinaryOp` for `Int` doesn't check for zero divisor. Runtime panic with confusing message instead of clean error. (`vm/FIXME.md` §10)~~                                                                                                                                              | ~~Correctness~~       | ~~high~~         |
| 27 | ~~`Abort()` TOCTOU between `atomic.Load` and `Lock`~~                             | ~~Two concurrent `Abort()` calls can both pass the check and both call `cancel()` + recursive `cvm.Abort()`. Use `atomic.CompareAndSwap` or move check inside lock. (`vm/FIXME.md` §11)~~                                                                                              | ~~Thread Safety~~     | ~~high~~         |
| 28 | ~~`checkGrowStack` sets `v.err` but execution continues~~                         | ~~After stack overflow detection, `run()` continues with the too-small stack, potentially causing OOB access before the next abort check. (`vm/FIXME.md` §12)~~                                                                                                                        | ~~Correctness~~       | ~~high~~         |
| 29 | ~~`Program.Equals()` doesn't lock `other`~~                                   | ~~Read lock acquired on `self` but `other` fields accessed without synchronisation. Data race if `other` is being modified concurrently. (`FIXME.md` §1.3)~~                                                                                                                        | ~~Thread Safety~~     | ~~high~~         |
| 30 | ~~VM defaults `In`/`Out`/`Args` to OS globals — never overridden~~                | ~~`NewVM` defaults I/O to `os.Stdin`/`os.Stdout` and `Args` to `os.Args`. Root package never overrides. Scripts see host binary's argument list. Security: leaks host args to sandboxed scripts. (`FIXME.md` §2.2) → #31~~                                                             | ~~Integration~~       | ~~high~~         |
| 31 | ~~CLI cannot forward arguments to scripts~~                                       | ~~`run` subcommand accepts exactly one arg (the file). No mechanism to pass args to scripts. No `--` separator. (`FIXME.md` §3.2) → #30~~                                                                                                                                              | ~~Correctness~~       | ~~high~~         |
| 32 | Debug `fmt.Println` left in `main()`                                          | `fmt.Println(rumo.AllModuleNames())` prints on every CLI invocation. Pollutes output, breaks piped workflows. (`FIXME.md` §3.3)                                                                                                                                                    | Maintenance       | high         |
| 33 | `Unmarshal` hardcoded to global `Modules`                                     | `p.bytecode.Unmarshal(body[n:], Modules)` always uses the global module map. Embedders with custom modules can't inject them into deserialization. Security: compiled bytecode inherits all available modules at runtime. (`FIXME.md` §2.3)                                        | Integration       | high         |
| 34 | No version field in binary format                                             | 4-byte magic but no version. Bytecode format changes produce cryptic unmarshal errors instead of "incompatible version" message. (`FIXME.md` §4.1)                                                                                                                                 | Compatibility     | high         |
| 35 | Deserialized `BuiltinFunction` objects are non-functional                     | `UnmarshalObject` restores `Name` but not `Value` (the Go function). `fixDecodedObject` doesn't resolve builtins by name. Calling a deserialized builtin causes nil-pointer panic. (`FIXME.md` §4.2, `vm/FIXME.md` §17)                                                            | Compatibility     | high         |
| 36 | `rand.Seed` is deprecated (Go 1.20+)                                          | `rand.Seed` is a no-op on Go 1.20+. Module still exposes it. Test relies on `GODEBUG` override. (`std/FIXME.md` §3)                                                                                                                                                                | Compatibility     | high         |
| 38 | `strings.Title` is deprecated (Go 1.18+)                                      | `strings.Title` doesn't handle Unicode correctly (e.g., Dutch "ij" digraph). Should use `golang.org/x/text/cases`. (`std/FIXME.md` §4)                                                                                                                                             | Compatibility     | high         |
| 39 | Code generator backtick injection in `.rumo` source embedding                 | A backtick in any `.rumo` source module breaks Go raw string literals in generated `stdlib.go`, causing a build failure. (`std/FIXME.md` §5)                                                                                                                                       | Correctness       | high         |
| 40 | JSON `Encode` silently drops unknown `Object` types                           | Unknown types produce no output. Can generate malformed JSON (e.g., `[,1]`). (`std/FIXME.md` §6)                                                                                                                                                                                   | Correctness       | high         |
| 41 | `fmt` print functions panic on missing VM in context                          | Unchecked type assertion on context value. Nil-pointer panic if VM key is absent. (`std/FIXME.md` §7) → #3                                                                                                                                                                         | Correctness       | high         |
| 60 | `builtinRange` has no upper-bound limit                                       | `range(0, 9999999999)` allocates billions of objects. Alloc counter only applies inside `run()`, not in builtins. Trivial DoS vector. (`vm/FIXME.md` §13)                                                                                                                          | Security          | medium       |
| 61 | `time.After` leak in `wait()`                                                 | `time.After` timer not garbage collected until it fires. Leaks memory for early returns. (`vm/FIXME.md` §14)                                                                                                                                                                       | Performance       | medium       |
| 62 | `Importable.Import` returns bare `interface{}`                                | Contract is implicit and type-switched. Easily breakable. (`vm/FIXME.md` §15)                                                                                                                                                                                                      | Maintenance       | medium       |
| 63 | Builtin function registration via `init()` is order-dependent                 | `init()` ordering depends on source file names. Renaming a file changes builtin index mapping, breaking serialised bytecode. (`vm/FIXME.md` §16) → #35                                                                                                                             | Compatibility     | medium       |
| 64 | `Unmarshal` not atomic on failure                                             | Fields overwritten sequentially. Partial failure leaves `Program` in inconsistent state. (`FIXME.md` §1.4)                                                                                                                                                                         | Thread Safety     | medium       |
| 65 | REPL source module imports run in isolated VMs                                | Each source module import creates a separate VM. Closures referencing module-level variables are orphaned. Import errors silently swallowed. (`FIXME.md` §2.4)                                                                                                                     | Correctness       | medium       |
| 66 | `compileSrc` unconditionally enables file imports                             | CLI always enables file imports. Inconsistent permission model between CLI and compiled programs. (`FIXME.md` §2.5)                                                                                                                                                                | Security          | medium       |
| 67 | "Error compiling" message for runtime errors                                  | `CompileAndRun` can fail during either compilation or execution, but error message always says "Error compiling". (`FIXME.md` §3.4)                                                                                                                                                | Correctness       | medium       |
| 68 | Build output path inconsistent between CLI and public API                     | CLI produces `script.rumo.out`, public API produces `script.out` for the same input. (`FIXME.md` §3.5)                                                                                                                                                                             | Correctness       | medium       |
| 69 | REPL ignores context cancellation                                             | REPL loop never checks `ctx.Done()`. Context cancellation silently ignored for interactive terminals. (`FIXME.md` §3.6) → #24                                                                                                                                                      | Correctness       | medium       |
| 70 | REPL constants accumulate without bounds                                      | `constants` slice grows every iteration with no deduplication or compaction. Memory consumed monotonically in long sessions. (`FIXME.md` §5.1)                                                                                                                                     | Performance       | medium       |
| 71 | `text.repeat` panics on negative count                                        | `strings.Repeat` panics on negative count. `len(s)*count` can overflow on 32-bit. (`std/FIXME.md` §8)                                                                                                                                                                              | Correctness       | medium       |
| 72 | `text.format_float` panics on empty format string                             | Accessing `s2[0]` on empty string panics with index out of range. (`std/FIXME.md` §9)                                                                                                                                                                                              | Correctness       | medium       |
| 73 | `text.substr` operates on byte indices, not rune indices                      | Multi-byte UTF-8 strings sliced by bytes. `substr("日本語", 0, 2)` returns incomplete character. (`std/FIXME.md` §10)                                                                                                                                                                 | Correctness       | medium       |
| 74 | JSON decoder returns all numbers as `Float`                                   | All JSON numeric literals decoded as `*vm.Float`. `json.decode(json.encode(42))` produces `42.0`. Type fidelity lost on round-trip. (`std/FIXME.md` §11)                                                                                                                           | Correctness       | medium       |
| 75 | `wrapError` duplication in `vm/module`                                        | Unexported `wrapError` and exported `WrapError` have identical logic. (`std/FIXME.md` §12)                                                                                                                                                                                         | Maintenance       | medium       |
| 76 | Copy-paste argument error-message bugs                                        | Three error messages reference wrong argument name/index: `funcASSRSs`, `funcASSRI` (builtin.go), `timesBefore` (times.go). (`std/FIXME.md` §13)                                                                                                                                   | Correctness       | medium       |
| 77 | `times.date` non-deterministically uses local timezone                        | `time.Now().Location()` depends on system timezone at call time. Non-deterministic, platform-dependent results. (`std/FIXME.md` §14)                                                                                                                                               | Correctness       | medium       |
| 78 | `ParseExport` and module registration panic on invalid input                  | Typos in function definition strings crash the application at startup with a panic stack trace. (`std/FIXME.md` §15)                                                                                                                                                               | Correctness       | medium       |
| 106 | `ImmutableArray.BinaryOp(Add)` mutates receiver                               | `append(o.Value, rhs.Value...)` mutates the underlying slice when spare capacity exists. Violates "immutable" contract. (`vm/FIXME.md` §18)                                                                                                                                        | Correctness       | low          |
| 107 | `String.IndexGet` data race on lazy `runeStr` init                            | Lazy initialisation of `runeStr` field on the receiver pointer. Under concurrency (shared String in globals), data race. (`vm/FIXME.md` §19) → #1, #25                                                                                                                             | Thread Safety     | low          |
| 108 | `ObjectImpl.TypeName()` and `String()` panic                                  | Forgotten override crashes the VM instead of returning a sentinel. (`vm/FIXME.md` §20)                                                                                                                                                                                             | Correctness       | low          |
| 109 | Typo in `indexAssign` error message                                           | `"invaid index value type"` — should be `"invalid"`. (`vm/FIXME.md` §21)                                                                                                                                                                                                           | Maintenance       | low          |
| 110 | `MaxStringLen`/`MaxBytesLen` are `var` — concurrent mutation possible         | Package-level `var` instead of `atomic`. Any embedder can mutate concurrently. (`vm/FIXME.md` §22)                                                                                                                                                                                 | Thread Safety     | low          |
| 111 | `AllModuleNames()` returns non-deterministic order                            | Iterates Go maps. Module names in random order each run. Affects REPL help, CLI output, test stability. (`FIXME.md` §7.2)                                                                                                                                                          | Maintenance       | low          |
| 112 | `BuiltinModules` comment says "source type"                                   | `stdlib.go:19` comment for `BuiltinModules` says "source type standard library modules" — should say "builtin type". (`FIXME.md` §7.3)                                                                                                                                             | Maintenance       | low          |
| 113 | `variable.go` doc comments say "returns 0"                                    | `Array()` and `Map()` doc comments say "returns 0 if not convertible" — they return `nil`. (`FIXME.md` §7.4)                                                                                                                                                                       | Maintenance       | low          |
| 114 | `prepCompile` panics on symbol index mismatch                                 | Internal consistency check uses `panic` instead of error return. Crashes embedding applications. (`FIXME.md` §7.5)                                                                                                                                                                 | Correctness       | low          |
| 115 | `TestRunCompiledFileExecutesOnlyOnce` commented out                           | Suggests compiled execution has an unresolved issue. (`FIXME.md` §7.6)                                                                                                                                                                                                             | Maintenance       | low          |
| 116 | `TestModulesRun` commented out                                                | References removed `os` module. Dead test code. (`FIXME.md` §7.7)                                                                                                                                                                                                                  | Maintenance       | low          |
| 117 | Dead code in `builtin.go` (`funcARI64`, `funcAI64R`)                          | Defined but never called — other adapters handle these types. (`std/FIXME.md` §16)                                                                                                                                                                                                 | Maintenance       | low          |
| 118 | Code generator bugs in `std/main.go`                                          | `cdRoot` ignores `os.Chdir` errors. `len(os.Args) < 1` always false. `os.ReadDir` error silently discarded. (`std/FIXME.md` §17)                                                                                                                                                   | Correctness       | low          |
| 119 | `text.re_match` recompiles regex on every call                                | Unlike `re_compile` which returns reusable `Regexp`. Performance implication undocumented. (`std/FIXME.md` §18)                                                                                                                                                                    | Performance       | low          |
| 120 | JSON map key ordering non-deterministic                                       | `Encode` iterates Go maps directly. Different byte output on each call. Test flakiness. (`std/FIXME.md` §19)                                                                                                                                                                       | Correctness       | low          |
| 121 | `text.pad_left`/`pad_right` arithmetic can produce incorrect results          | Rounding-down when `padStrLen > 1` may produce short padding. (`std/FIXME.md` §20)                                                                                                                                                                                                 | Correctness       | low          |

