---
title: Standard Library - math
---

## Import

```golang
math := import("math")
```

## Constants

- `e`
- `pi`
- `phi`
- `sqrt2`
- `sqrtE`
- `sqrtPi`
- `sqrtPhi`
- `ln2`
- `log2E`
- `ln10`
- `log10E`

## Functions

- `abs(x float) => float`
- `acos(x float) => float`
- `acosh(x float) => float`
- `asin(x float) => float`
- `asinh(x float) => float`
- `atan(x float) => float`
- `atan2(y, x float) => float`
- `atanh(x float) => float`
- `cbrt(x float) => float`
- `ceil(x float) => float`
- `copysign(x, y float) => float`
- `cos(x float) => float`
- `cosh(x float) => float`
- `dim(x, y float) => float`
- `erf(x float) => float`
- `erfc(x float) => float`
- `exp(x float) => float`
- `exp2(x float) => float`
- `expm1(x float) => float`
- `floor(x float) => float`
- `gamma(x float) => float`
- `hypot(x, y float) => float`
- `ilogb(x float) => int`
- `inf(sign int) => float`
- `is_inf(x float, sign int) => bool`
- `is_nan(x float) => bool`
- `j0(x float) => float`
- `j1(x float) => float`
- `jn(n int, x float) => float`
- `ldexp(frac float, exp int) => float`
- `log(x float) => float`
- `log10(x float) => float`
- `log1p(x float) => float`
- `log2(x float) => float`
- `logb(x float) => float`
- `max(x, y float) => float`
- `min(x, y float) => float`
- `mod(x, y float) => float`
- `nan() => float`
- `nextafter(x, y float) => float`
- `pow(x, y float) => float`
- `pow10(n int) => float`
- `remainder(x, y float) => float`
- `signbit(x float) => bool`
- `sin(x float) => float`
- `sinh(x float) => float`
- `sqrt(x float) => float`
- `tan(x float) => float`
- `tanh(x float) => float`
- `trunc(x float) => float`
- `y0(x float) => float`
- `y1(x float) => float`
- `yn(n int, x float) => float`
